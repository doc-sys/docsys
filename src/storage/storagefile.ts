class StorageFile {
    id: string;
    name: string;
    created: string;

    constructor(id: string, name: string, created: string) {
        this.id = id;
        this.name = name;
        this.created = created;
    }
}